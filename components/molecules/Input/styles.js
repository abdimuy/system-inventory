import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    borderColor: '#ccc',
    borderWidth: 1,
    borderRadius: 5,
    width: 60,
    fontSize: 20,
    padding: 0,
    height: 40,
    textAlign: 'center',
    textAlignVertical: 'center',
  }
})
import { StyleSheet} from 'react-native';

export default StyleSheet.create({
  text: {
    textAlign: 'center',
    fontSize: 26,
    color: '#000',
    marginVertical: 18,
    width: '100%',
  },
  subtitle: {
    textAlign: 'center', 
    fontSize: 20,
    color: '#000',
    marginBottom: 18,
    width: '100%',
  }
});